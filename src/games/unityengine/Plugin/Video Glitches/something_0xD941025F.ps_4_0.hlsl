#include "../../common.hlsl"

Texture2D<float4> t0 : register(t0);
SamplerState s0_s : register(s0);
cbuffer cb1 : register(b1){
  float4 cb1[1];
}
cbuffer cb0 : register(b0){
  float4 cb0[6];
}

#define cmp -

void main(
  float4 v0 : SV_POSITION0,
  float2 v1 : TEXCOORD0,
  out float4 o0 : SV_Target0)
{
  float4 r0,r1,r2,r3,r4,r5,r6;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.xyzw = t0.Sample(s0_s, v1.xy).xyzw;
  r0.xyz = renodx::color::srgb::EncodeSafe(r0.xyz);
  r1.xw = v1.yy;
  r1.y = cb1[0].y;
  r0.w = 0;
  r2.x = 1;
  while (true) {
    r2.y = cmp((int)r2.x >= 9);
    if (r2.y != 0) break;
    r2.y = (int)r2.x;
    r2.z = exp2(r2.y);
    r2.yw = r2.yy + r1.xy;
    r3.x = r2.z + r2.z;
    r3.yz = r3.xx * r2.yw;
    r4.xy = floor(r3.yz);
    r4.xy = r4.xy / r3.xx;
    r3.w = dot(r4.xy, float2(12.9898005,78.2330017));
    r3.w = sin(r3.w);
    r3.w = 43758.5469 * r3.w;
    r3.w = frac(r3.w);
    r4.xyzw = r2.ywyw * r3.xxxx + float4(1,0,0,1);
    r4.xyzw = floor(r4.xyzw);
    r4.xyzw = r4.xyzw / r3.xxxx;
    r4.x = dot(r4.xy, float2(12.9898005,78.2330017));
    r4.x = sin(r4.x);
    r4.x = 43758.5469 * r4.x;
    r4.y = dot(r4.zw, float2(12.9898005,78.2330017));
    r4.y = sin(r4.y);
    r4.y = 43758.5469 * r4.y;
    r4.xy = frac(r4.xy);
    r2.yw = r2.yw * r3.xx + float2(1,1);
    r2.yw = floor(r2.yw);
    r2.yw = r2.yw / r3.xx;
    r2.y = dot(r2.yw, float2(12.9898005,78.2330017));
    r2.y = sin(r2.y);
    r2.y = 43758.5469 * r2.y;
    r2.y = frac(r2.y);
    r4.zw = cmp(r3.yz >= -r3.yz);
    r3.xy = frac(abs(r3.yz));
    r3.xy = r4.zw ? r3.xy : -r3.xy;
    r3.xy = max(float2(0,0), r3.xy);
    r4.zw = r3.xy * float2(-2,-2) + float2(3,3);
    r3.xy = r3.xy * r3.xy;
    r5.xy = r4.zw * r3.xy;
    r3.xy = -r4.zw * r3.xy + float2(1,1);
    r2.w = r5.x * r4.x;
    r2.w = r3.w * r3.x + r2.w;
    r2.y = r5.x * r2.y;
    r2.y = r4.y * r3.x + r2.y;
    r2.y = r2.y * r5.y;
    r2.y = r2.w * r3.y + r2.y;
    r2.y = r2.y / r2.z;
    r0.w = r2.y + r0.w;
    r2.x = (int)r2.x + 1;
  }
  r0.w = -0.5 + r0.w;
  r0.w = cb0[3].y * r0.w;
  r0.w = r0.w * 0.0399999991 + v1.x;
  r2.x = 100 * v1.y;
  r2.yw = float2(10,10) * cb1[0].yy;
  r1.xy = float2(0,1.40129846e-045);
  while (true) {
    r3.x = cmp((int)r1.y >= 9);
    if (r3.x != 0) break;
    r3.x = (int)r1.y;
    r3.y = exp2(r3.x);
    r3.xz = r3.xx + r2.xy;
    r3.w = r3.y + r3.y;
    r4.xy = r3.xz * r3.ww;
    r4.zw = floor(r4.xy);
    r4.zw = r4.zw / r3.ww;
    r4.z = dot(r4.zw, float2(12.9898005,78.2330017));
    r4.z = sin(r4.z);
    r4.z = 43758.5469 * r4.z;
    r5.xyzw = r3.xzxz * r3.wwww + float4(1,0,0,1);
    r5.xyzw = floor(r5.xyzw);
    r5.xyzw = r5.xyzw / r3.wwww;
    r4.w = dot(r5.xy, float2(12.9898005,78.2330017));
    r4.w = sin(r4.w);
    r4.w = 43758.5469 * r4.w;
    r4.zw = frac(r4.zw);
    r5.x = dot(r5.zw, float2(12.9898005,78.2330017));
    r5.x = sin(r5.x);
    r5.x = 43758.5469 * r5.x;
    r5.x = frac(r5.x);
    r3.xz = r3.xz * r3.ww + float2(1,1);
    r3.xz = floor(r3.xz);
    r3.xz = r3.xz / r3.ww;
    r3.x = dot(r3.xz, float2(12.9898005,78.2330017));
    r3.x = sin(r3.x);
    r3.x = 43758.5469 * r3.x;
    r3.x = frac(r3.x);
    r3.zw = cmp(r4.xy >= -r4.xy);
    r4.xy = frac(abs(r4.xy));
    r3.zw = r3.zw ? r4.xy : -r4.xy;
    r3.zw = max(float2(0,0), r3.zw);
    r4.xy = r3.zw * float2(-2,-2) + float2(3,3);
    r3.zw = r3.zw * r3.zw;
    r5.yz = r4.xy * r3.zw;
    r3.zw = -r4.xy * r3.zw + float2(1,1);
    r4.x = r5.y * r4.w;
    r4.x = r4.z * r3.z + r4.x;
    r3.x = r5.y * r3.x;
    r3.x = r5.x * r3.z + r3.x;
    r3.x = r3.x * r5.z;
    r3.x = r4.x * r3.w + r3.x;
    r3.x = r3.x / r3.y;
    r1.x = r3.x + r1.x;
    r1.y = (int)r1.y + 1;
  }
  r1.x = -0.5 + r1.x;
  r1.x = cb0[3].z * r1.x;
  r0.w = r1.x * 0.100000001 + r0.w;
  r1.x = cb1[0].y * cb0[4].x;
  r1.x = 3.14159274 * r1.x;
  r1.x = v1.y * cb0[3].w + -r1.x;
  r1.x = sin(r1.x);
  r1.x = -0.920000017 + r1.x;
  r1.y = 0;
  r2.x = 1;
  while (true) {
    r2.y = cmp((int)r2.x >= 9);
    if (r2.y != 0) break;
    r2.y = (int)r2.x;
    r3.x = exp2(r2.y);
    r2.y = cb1[0].y + r2.y;
    r3.y = r3.x + r3.x;
    r3.z = r3.y * r2.y;
    r3.w = floor(r3.z);
    r3.w = r3.w / r3.y;
    r3.w = dot(r3.ww, float2(12.9898005,78.2330017));
    r3.w = sin(r3.w);
    r3.w = 43758.5469 * r3.w;
    r4.xy = r2.yy * r3.yy + float2(0,1);
    r4.xy = floor(r4.xy);
    r4.xy = r4.xy / r3.yy;
    r2.y = dot(r4.yx, float2(12.9898005,78.2330017));
    r2.y = sin(r2.y);
    r2.y = 43758.5469 * r2.y;
    r2.y = frac(r2.y);
    r3.y = dot(r4.xy, float2(12.9898005,78.2330017));
    r3.y = sin(r3.y);
    r3.y = 43758.5469 * r3.y;
    r3.yw = frac(r3.yw);
    r4.x = dot(r4.yy, float2(12.9898005,78.2330017));
    r4.x = sin(r4.x);
    r4.x = 43758.5469 * r4.x;
    r4.x = frac(r4.x);
    r4.y = cmp(r3.z >= -r3.z);
    r3.z = frac(abs(r3.z));
    r3.z = r4.y ? r3.z : -r3.z;
    r3.z = max(0, r3.z);
    r4.y = r3.z * -2 + 3;
    r3.z = r3.z * r3.z;
    r4.z = r4.y * r3.z;
    r3.z = -r4.y * r3.z + 1;
    r2.y = r4.z * r2.y;
    r2.y = r3.w * r3.z + r2.y;
    r3.w = r4.x * r4.z;
    r3.y = r3.y * r3.z + r3.w;
    r3.y = r3.y * r4.z;
    r2.y = r2.y * r3.z + r3.y;
    r2.y = r2.y / r3.x;
    r1.y = r2.y + r1.y;
    r2.x = (int)r2.x + 1;
  }
  r1.x = r1.x * r1.y;
  r1.x = max(0, r1.x);
  r1.x = min(0.00999999978, r1.x);
  r1.x = cb0[4].y * r1.x;
  r2.z = cb0[4].z * v1.y;
  r1.y = 0;
  r2.x = 1;
  while (true) {
    r2.y = cmp((int)r2.x >= 9);
    if (r2.y != 0) break;
    r2.y = (int)r2.x;
    r3.x = exp2(r2.y);
    r3.yz = r2.zw + r2.yy;
    r2.y = r3.x + r3.x;
    r4.xy = r3.yz * r2.yy;
    r4.zw = floor(r4.xy);
    r4.zw = r4.zw / r2.yy;
    r3.w = dot(r4.zw, float2(12.9898005,78.2330017));
    r3.w = sin(r3.w);
    r3.w = 43758.5469 * r3.w;
    r3.w = frac(r3.w);
    r5.xyzw = r3.yzyz * r2.yyyy + float4(1,0,0,1);
    r5.xyzw = floor(r5.xyzw);
    r5.xyzw = r5.xyzw / r2.yyyy;
    r4.z = dot(r5.xy, float2(12.9898005,78.2330017));
    r4.z = sin(r4.z);
    r4.z = 43758.5469 * r4.z;
    r4.w = dot(r5.zw, float2(12.9898005,78.2330017));
    r4.w = sin(r4.w);
    r4.w = 43758.5469 * r4.w;
    r4.zw = frac(r4.zw);
    r3.yz = r3.yz * r2.yy + float2(1,1);
    r3.yz = floor(r3.yz);
    r3.yz = r3.yz / r2.yy;
    r2.y = dot(r3.yz, float2(12.9898005,78.2330017));
    r2.y = sin(r2.y);
    r2.y = 43758.5469 * r2.y;
    r2.y = frac(r2.y);
    r3.yz = cmp(r4.xy >= -r4.xy);
    r4.xy = frac(abs(r4.xy));
    r3.yz = r3.yz ? r4.xy : -r4.xy;
    r3.yz = max(float2(0,0), r3.yz);
    r4.xy = r3.yz * float2(-2,-2) + float2(3,3);
    r3.yz = r3.yz * r3.yz;
    r5.xy = r4.xy * r3.yz;
    r3.yz = -r4.xy * r3.yz + float2(1,1);
    r4.x = r5.x * r4.z;
    r3.w = r3.w * r3.y + r4.x;
    r2.y = r5.x * r2.y;
    r2.y = r4.w * r3.y + r2.y;
    r2.y = r2.y * r5.y;
    r2.y = r3.w * r3.z + r2.y;
    r2.y = r2.y / r3.x;
    r1.y = r2.y + r1.y;
    r2.x = (int)r2.x + 1;
  }
  r1.y = -0.5 + r1.y;
  r1.y = max(0, r1.y);
  r0.w = -r1.y * r1.x + r0.w;
  r1.y = 5 + r1.y;
  r2.x = 1 + -v1.y;
  r2.x = log2(r2.x);
  r2.x = 500 * r2.x;
  r2.x = exp2(r2.x);
  r2.x = 1000 * r2.x;
  r2.x = min(1, r2.x);
  r2.x = cb0[4].w * r2.x;
  r2.y = -r2.x * 0.5 + 1;
  r3.x = 100 * v1.y;
  r3.y = 10 * cb1[0].y;
  r2.zw = float2(0,1.40129846e-045);
  while (true) {
    r3.z = cmp((int)r2.w >= 9);
    if (r3.z != 0) break;
    r3.z = (int)r2.w;
    r3.w = exp2(r3.z);
    r4.xy = r3.xy + r3.zz;
    r3.z = r3.w + r3.w;
    r4.zw = r4.xy * r3.zz;
    r5.xy = floor(r4.zw);
    r5.xy = r5.xy / r3.zz;
    r5.x = dot(r5.xy, float2(12.9898005,78.2330017));
    r5.x = sin(r5.x);
    r5.x = 43758.5469 * r5.x;
    r6.xyzw = r4.xyxy * r3.zzzz + float4(1,0,0,1);
    r6.xyzw = floor(r6.xyzw);
    r6.xyzw = r6.xyzw / r3.zzzz;
    r5.y = dot(r6.xy, float2(12.9898005,78.2330017));
    r5.y = sin(r5.y);
    r5.y = 43758.5469 * r5.y;
    r5.z = dot(r6.zw, float2(12.9898005,78.2330017));
    r5.z = sin(r5.z);
    r5.z = 43758.5469 * r5.z;
    r5.xyz = frac(r5.xyz);
    r4.xy = r4.xy * r3.zz + float2(1,1);
    r4.xy = floor(r4.xy);
    r4.xy = r4.xy / r3.zz;
    r3.z = dot(r4.xy, float2(12.9898005,78.2330017));
    r3.z = sin(r3.z);
    r3.z = 43758.5469 * r3.z;
    r3.z = frac(r3.z);
    r4.xy = cmp(r4.zw >= -r4.zw);
    r4.zw = frac(abs(r4.zw));
    r4.xy = r4.xy ? r4.zw : -r4.zw;
    r4.xy = max(float2(0,0), r4.xy);
    r4.zw = r4.xy * float2(-2,-2) + float2(3,3);
    r4.xy = r4.xy * r4.xy;
    r6.xy = r4.zw * r4.xy;
    r4.xy = -r4.zw * r4.xy + float2(1,1);
    r4.z = r6.x * r5.y;
    r4.z = r5.x * r4.x + r4.z;
    r3.z = r6.x * r3.z;
    r3.z = r5.z * r4.x + r3.z;
    r3.z = r3.z * r6.y;
    r3.z = r4.z * r4.y + r3.z;
    r3.z = r3.z / r3.w;
    r2.z = r3.z + r2.z;
    r2.w = (int)r2.w + 1;
  }
  r2.z = r2.z * 0.200000003 + 0.100000001;
  r2.y = r2.x * r2.z + r2.y;
  r1.z = r2.y * r0.w;
  r0.w = r1.y * r1.x + r2.x;
  r2.xyzw = t0.Sample(s0_s, r1.zw).xyzw;
  r2.xyz = renodx::color::srgb::EncodeSafe(r2.xyz);
  r1.x = cos(r0.w);
  r3.z = max(0, r1.x);
  r1.xy = float2(-2.09439516,-4.18879032) + r0.ww;
  r1.xy = cos(r1.xy);
  r3.xy = max(float2(0,0), r1.xy);
  r4.x = dot(r2.yzx, r3.xyz);
  r4.y = dot(r2.zxy, r3.xyz);
  r4.z = dot(r2.xyz, r3.xyz);
  r0.w = cmp(0 < cb0[5].z);
  if (r0.w != 0) {
    r0.w = 0.600000024 / cb0[5].z;
    r1.y = 0;
    r2.xyz = r4.xyz;
    r1.x = 0;
    while (true) {
      r2.w = cmp(r1.x >= cb0[5].z);
      if (r2.w != 0) break;
      r3.xy = r1.xy * float2(0.00700000022,0.00700000022) + r1.zw;
      r3.xyzw = t0.Sample(s0_s, r3.xy).xyzw;
      r2.xyz = r3.xyz * r0.www + r2.xyz;
      r1.x = 1 + r1.x;
    }
    r4.xyz = r2.xyz;
  }
  r1.y = cb1[0].y * cb0[5].y + v1.y;
  r1.x = 0;
  r0.w = 0;
  r1.z = 1;
  while (true) {
    r1.w = cmp((int)r1.z >= 9);
    if (r1.w != 0) break;
    r1.w = (int)r1.z;
    r2.x = exp2(r1.w);
    r2.yz = r1.xy + r1.ww;
    r1.w = r2.x + r2.x;
    r3.xy = r2.yz * r1.ww;
    r3.xz = floor(r3.xy);
    r3.xz = r3.xz / r1.ww;
    r2.w = dot(r3.xz, float2(12.9898005,78.2330017));
    r2.w = sin(r2.w);
    r2.w = 43758.5469 * r2.w;
    r2.w = frac(r2.w);
    r2.yz = r2.yz * r1.ww + float2(0,1);
    r2.yz = floor(r2.yz);
    r2.yz = r2.yz / r1.ww;
    r1.w = dot(r2.yz, float2(12.9898005,78.2330017));
    r1.w = sin(r1.w);
    r1.w = 43758.5469 * r1.w;
    r1.w = frac(r1.w);
    r2.y = cmp(r3.y >= -r3.y);
    r2.z = frac(abs(r3.y));
    r2.y = r2.y ? r2.z : -r2.z;
    r2.y = max(0, r2.y);
    r2.z = r2.y * -2 + 3;
    r2.y = r2.y * r2.y;
    r3.x = r2.z * r2.y;
    r2.y = -r2.z * r2.y + 1;
    r1.w = r3.x * r1.w;
    r1.w = r2.w * r2.y + r1.w;
    r1.w = r1.w / r2.x;
    r0.w = r1.w + r0.w;
    r1.z = (int)r1.z + 1;
  }
  r0.w = r0.w * cb0[5].x + -0.25;
  r0.w = max(0, r0.w);
  r0.w = min(0.100000001, r0.w);
  r0.w = 1 + r0.w;
  r1.xyz = r4.xyz * r0.www + float3(-0.5,-0.5,-0.5);
  r1.xyz = r1.xyz * cb0[2].zzz + cb0[2].yyy;
  r1.xyz = float3(0.5,0.5,0.5) + r1.xyz;
  if (injectedData.toneMapType == 0.f) {
    r1.xyz = saturate(r1.xyz);
  }
  r1.xyz = renodx::math::SignPow(r1.xyz, cb0[2].w);
  r0.w = dot(float3(0.298999995,0.587000012,0.114), r1.xyz);
  r1.xyz = r1.xyz + -r0.www;
  r1.xyz = cb0[3].xxx * r1.xyz + r0.www;
  r1.xyz = r1.xyz + -r0.xyz;
  r0.xyz = cb0[2].xxx * r1.xyz + r0.xyz;
  r0.xyz = renodx::color::srgb::DecodeSafe(r0.xyz);
  if (injectedData.countOld == injectedData.countNew) {
    r0.xyz = PostToneMapScale(r0.xyz);
  }
  o0.xyz = r0.xyz;
  o0.w = 1;
  return;
}