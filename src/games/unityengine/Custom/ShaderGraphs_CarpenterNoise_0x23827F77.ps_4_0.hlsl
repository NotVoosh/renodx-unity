#include "../shared.h"

Texture2D<float4> t0 : register(t0);
cbuffer cb1 : register(b1){
  float4 cb1[1];
}
cbuffer cb0 : register(b0){
  float4 cb0[123];
}

#define cmp -

void main(
  float4 v0 : SV_POSITION0,
  float4 v1 : INTERP0,
  float4 v2 : INTERP1,
  out float4 o0 : SV_TARGET0)
{
  float4 r0,r1,r2,r3,r4;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.x = cmp(cb0[19].x >= -cb0[19].x);
  r0.y = frac(abs(cb0[19].x));
  r0.x = r0.x ? r0.y : -r0.y;
  r0.x = 10000 * r0.x;
  r0.xy = v1.xy * cb0[23].xy + r0.xx;
  r0.xy = cb1[0].xx * r0.xy;
  r0.zw = floor(r0.xy);
  r0.xy = frac(r0.xy);
  r1.xy = float2(1,1) + r0.zw;
  r2.xyzw = float4(289,289,289,289) * r1.xyxy;
  r2.xyzw = cmp(r2.xyzw >= -r2.zwzw);
  r2.xyzw = r2.xyzw ? float4(289,289,0.00346020772,0.00346020772) : float4(-289,-289,-0.00346020772,-0.00346020772);
  r1.xy = r2.zw * r1.xy;
  r1.xy = frac(r1.xy);
  r1.xy = r2.xy * r1.xy;
  r1.z = r1.x * 34 + 1;
  r1.x = r1.z * r1.x;
  r1.z = 289 * r1.x;
  r1.z = cmp(r1.z >= -r1.z);
  r1.zw = r1.zz ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r1.x = r1.x * r1.w;
  r1.x = frac(r1.x);
  r1.x = r1.z * r1.x + r1.y;
  r1.y = r1.x * 34 + 1;
  r1.x = r1.y * r1.x;
  r1.y = 289 * r1.x;
  r1.y = cmp(r1.y >= -r1.y);
  r1.yz = r1.yy ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r1.x = r1.x * r1.z;
  r1.x = frac(r1.x);
  r1.x = r1.y * r1.x;
  r1.x = 0.024390243 * r1.x;
  r1.x = frac(r1.x);
  r1.xy = r1.xx * float2(2,2) + float2(-1,-0.5);
  r1.y = floor(r1.y);
  r2.x = r1.x + -r1.y;
  r2.y = -0.5 + abs(r1.x);
  r1.x = dot(r2.xy, r2.xy);
  r1.x = rsqrt(r1.x);
  r1.xy = r2.xy * r1.xx;
  r1.zw = float2(-1,-1) + r0.xy;
  r1.x = dot(r1.xy, r1.zw);
  r2.xyzw = float4(0,1,1,0) + r0.zwzw;
  r3.xyzw = float4(289,289,289,289) * r2.xyzw;
  r3.xyzw = cmp(r3.xyzw >= -r3.xyzw);
  r4.xyzw = r3.zwzw ? float4(289,289,0.00346020772,0.00346020772) : float4(-289,-289,-0.00346020772,-0.00346020772);
  r3.xyzw = r3.xyxy ? float4(289,289,0.00346020772,0.00346020772) : float4(-289,-289,-0.00346020772,-0.00346020772);
  r1.yz = r4.zw * r2.zw;
  r2.xy = r3.zw * r2.xy;
  r2.xy = frac(r2.xy);
  r2.xy = r3.xy * r2.xy;
  r1.yz = frac(r1.yz);
  r1.yz = r4.xy * r1.yz;
  r1.w = r1.y * 34 + 1;
  r1.y = r1.w * r1.y;
  r1.w = 289 * r1.y;
  r1.w = cmp(r1.w >= -r1.w);
  r2.zw = r1.ww ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r1.y = r2.w * r1.y;
  r1.y = frac(r1.y);
  r1.y = r2.z * r1.y + r1.z;
  r1.z = r1.y * 34 + 1;
  r1.y = r1.z * r1.y;
  r1.z = 289 * r1.y;
  r1.z = cmp(r1.z >= -r1.z);
  r1.zw = r1.zz ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r1.y = r1.y * r1.w;
  r1.y = frac(r1.y);
  r1.y = r1.z * r1.y;
  r1.y = 0.024390243 * r1.y;
  r1.y = frac(r1.y);
  r1.yz = r1.yy * float2(2,2) + float2(-1,-0.5);
  r1.z = floor(r1.z);
  r3.x = r1.y + -r1.z;
  r3.y = -0.5 + abs(r1.y);
  r1.y = dot(r3.xy, r3.xy);
  r1.y = rsqrt(r1.y);
  r1.yz = r3.xy * r1.yy;
  r3.xyzw = float4(-0,-1,-1,-0) + r0.xyxy;
  r1.y = dot(r1.yz, r3.zw);
  r1.x = r1.x + -r1.y;
  r1.zw = r0.xy * r0.xy;
  r1.zw = r1.zw * r0.xy;
  r2.zw = r0.xy * float2(6,6) + float2(-15,-15);
  r2.zw = r0.xy * r2.zw + float2(10,10);
  r1.zw = r2.zw * r1.zw;
  r1.x = r1.w * r1.x + r1.y;
  r4.xyzw = float4(289,289,289,289) * r0.zwzw;
  r4.xyzw = cmp(r4.xyzw >= -r4.zwzw);
  r4.xyzw = r4.xyzw ? float4(289,289,0.00346020772,0.00346020772) : float4(-289,-289,-0.00346020772,-0.00346020772);
  r0.zw = r4.zw * r0.zw;
  r0.zw = frac(r0.zw);
  r0.zw = r4.xy * r0.zw;
  r1.y = r0.z * 34 + 1;
  r0.z = r1.y * r0.z;
  r1.y = 289 * r0.z;
  r1.y = cmp(r1.y >= -r1.y);
  r2.zw = r1.yy ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r0.z = r2.w * r0.z;
  r0.z = frac(r0.z);
  r0.z = r2.z * r0.z + r0.w;
  r0.w = r0.z * 34 + 1;
  r0.z = r0.w * r0.z;
  r0.w = 289 * r0.z;
  r0.w = cmp(r0.w >= -r0.w);
  r2.zw = r0.ww ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r0.z = r2.w * r0.z;
  r0.z = frac(r0.z);
  r0.z = r2.z * r0.z;
  r0.z = 0.024390243 * r0.z;
  r0.z = frac(r0.z);
  r0.zw = r0.zz * float2(2,2) + float2(-1,-0.5);
  r0.w = floor(r0.w);
  r4.x = r0.z + -r0.w;
  r4.y = -0.5 + abs(r0.z);
  r0.z = dot(r4.xy, r4.xy);
  r0.z = rsqrt(r0.z);
  r0.zw = r4.xy * r0.zz;
  r0.x = dot(r0.zw, r0.xy);
  r0.y = r2.x * 34 + 1;
  r0.y = r0.y * r2.x;
  r0.z = 289 * r0.y;
  r0.z = cmp(r0.z >= -r0.z);
  r0.zw = r0.zz ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r0.y = r0.y * r0.w;
  r0.y = frac(r0.y);
  r0.y = r0.z * r0.y + r2.y;
  r0.z = r0.y * 34 + 1;
  r0.y = r0.z * r0.y;
  r0.z = 289 * r0.y;
  r0.z = cmp(r0.z >= -r0.z);
  r0.zw = r0.zz ? float2(289,0.00346020772) : float2(-289,-0.00346020772);
  r0.y = r0.y * r0.w;
  r0.y = frac(r0.y);
  r0.y = r0.z * r0.y;
  r0.y = 0.024390243 * r0.y;
  r0.y = frac(r0.y);
  r0.yz = r0.yy * float2(2,2) + float2(-1,-0.5);
  r0.z = floor(r0.z);
  r2.x = r0.y + -r0.z;
  r2.y = -0.5 + abs(r0.y);
  r0.y = dot(r2.xy, r2.xy);
  r0.y = rsqrt(r0.y);
  r0.yz = r2.xy * r0.yy;
  r0.y = dot(r0.yz, r3.xy);
  r0.y = r0.y + -r0.x;
  r0.x = r1.w * r0.y + r0.x;
  r0.y = r1.x + -r0.x;
  r0.x = r1.z * r0.y + r0.x;
  r0.x = 0.5 + r0.x;
  r0.y = cmp(r0.x >= 0.5);
  r0.z = r0.y ? 1.000000 : 0;
  r0.y = r0.y ? 0 : 1;
  r0.w = -r0.x * 2 + 1;
  r1.xy = cb0[122].xy * v1.xy;
  r1.xy = (uint2)r1.xy;
  r1.zw = float2(0,0);
  r1.xyzw = t0.Load(r1.xyz).xyzw;
  r2.xyz = sign(r1.xyz) * r1.xyz * r1.xyz;
  r2.xyz = r2.xyz * r0.www;
  r3.xyz = r1.xyz + r1.xyz;
  r2.xyz = r3.xyz * r0.xxx + r2.xyz;
  r2.xyz = r2.xyz * r0.yyy;
  r0.y = r0.x * 2 + -1;
  r0.x = 1 + -r0.x;
  r3.xyz = r3.xyz * r0.xxx;
  r4.xyz = renodx::math::SignSqrt(r1.xyz);
  r0.xyw = r4.xyz * r0.yyy + r3.xyz;
  r0.xyz = r0.xyw * r0.zzz + r2.xyz;
  r0.xyz = r0.xyz + -r1.xyz;
  r0.w = cb1[0].w + -cb1[0].y;
  r0.w = cb1[0].z * r0.w + cb1[0].y;
  o0.xyz = r0.www * r0.xyz + r1.xyz;
  o0.w = 1;
  return;
}