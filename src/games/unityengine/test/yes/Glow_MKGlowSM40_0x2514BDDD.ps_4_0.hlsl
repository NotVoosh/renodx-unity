#include "../../common.hlsl"

Texture2D<float4> t1 : register(t1);
Texture2D<float4> t0 : register(t0);
SamplerState s1_s : register(s1);
SamplerState s0_s : register(s0);
cbuffer cb0 : register(b0){
  float4 cb0[8];
}

void main(
  float4 v0 : SV_POSITION0,
  float2 v1 : TEXCOORD0,
  out float4 o0 : SV_Target0,
  out float4 o1 : SV_Target1,
  out float4 o2 : SV_Target2,
  out float4 o3 : SV_Target3)
{
  float4 r0,r1,r2,r3,r4,r5,r6;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.xy = cb0[3].xx * cb0[2].xx;
  r1.xy = float2(1,1) / r0.yy;
  r1.zw = float2(1,1) / cb0[3].yy;
  r1.xyzw = v1.xxyy * r1.xyzw + float4(-0.5,-0.5,-0.5,-0.5);
  r2.xyzw = frac(r1.yyww);
  r1.xyzw = -r2.xyzw + r1.xyzw;
  r2.xyz = float3(1,2,3) + -r2.yyy;
  r3.xyz = r2.xyz * r2.xyz;
  r4.xy = r3.xy * r2.xy;
  r4.yz = float2(4,4) * r4.xy;
  r5.xy = r2.zy * r3.zy + -r4.zy;
  r5.z = r4.x * 6 + r5.x;
  r2.x = -r2.x * r3.x + 6;
  r2.x = r2.x + -r5.y;
  r5.w = r2.x + -r5.z;
  r5.x = r4.x;
  r3.xyzw = float4(0.166666672,0.166666672,0.166666672,0.166666672) * r5.xyzw;
  r2.xyz = float3(1,2,3) + -r2.www;
  r4.xyz = r2.xyz * r2.xyz;
  r5.xy = r4.xy * r2.xy;
  r5.yz = float2(4,4) * r5.xy;
  r6.xy = r2.zy * r4.zy + -r5.zy;
  r6.z = r5.x * 6 + r6.x;
  r2.x = -r2.x * r4.x + 6;
  r2.x = r2.x + -r6.y;
  r6.w = r2.x + -r6.z;
  r6.x = r5.x;
  r2.xyzw = float4(0.166666672,0.166666672,0.166666672,0.166666672) * r6.xzyw;
  r4.xy = r3.xz + r3.yw;
  r4.zw = r2.xy + r2.zw;
  r1.xyzw = float4(-0.5,1.5,-0.5,1.5) + r1.xyzw;
  r2.xy = r3.yw;
  r2.xyzw = r2.xyzw / r4.xyzw;
  r1.xyzw = r2.xyzw + r1.xyzw;
  r0.zw = cb0[3].yy;
  r0.xyzw = r1.xyzw * r0.xyzw;
  r1.xyzw = t0.Sample(s0_s, r0.xz, int2(0, 0)).xyzw;
  r2.xyzw = t0.Sample(s0_s, r0.yz, int2(0, 0)).xyzw;
  r3.xyzw = t0.Sample(s0_s, r0.xw, int2(0, 0)).xyzw;
  r0.xyzw = t0.Sample(s0_s, r0.yw, int2(0, 0)).xyzw;
  if(injectedData.isClamped != 0.f){
    r1.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r1.xyz) : r1.xyz;
    r1.xyz = rolloffSdr(r1.xyz);
    r1.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r1.xyz) : r1.xyz;
    r2.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r2.xyz) : r2.xyz;
    r2.xyz = rolloffSdr(r2.xyz);
    r2.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r2.xyz) : r2.xyz;
    r3.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r3.xyz) : r3.xyz;
    r3.xyz = rolloffSdr(r3.xyz);
    r3.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r3.xyz) : r3.xyz;
    r0.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r0.xyz) : r0.xyz;
    r0.xyz = rolloffSdr(r0.xyz);
    r0.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r0.xyz) : r0.xyz;
  }
  r4.yw = r4.xz + r4.yw;
  r4.xy = r4.xz / r4.yw;
  r3.xyz = r3.xyz + -r0.xyz;
  r0.xyz = r4.xxx * r3.xyz + r0.xyz;
  r1.xyz = -r2.xyz + r1.xyz;
  r1.xyz = r4.xxx * r1.xyz + r2.xyz;
  r1.xyz = r1.xyz + -r0.xyz;
  r0.xyz = r4.yyy * r1.xyz + r0.xyz;
  r0.w = dot(r0.xyz, r0.xyz);
  r0.w = rsqrt(r0.w);
  r1.xyz = r0.xyz * r0.www;
  r2.xyz = cb0[4].yyy * r1.xyz;
  r0.xyz = max(float3(0,0,0), r0.xyz);
  r2.xyz = min(r0.xyz, r2.xyz);
  r3.xyz = cb0[3].zzz * float3(-0.0843257308,-0.325977564,0.0236618519) + float3(0.908999979,0.908999979,0.908999979);
  r2.xyz = r3.xyz * r2.xyz;
  r0.w = max(r2.y, r2.z);
  r0.w = max(r2.x, r0.w);
  r1.w = -cb0[4].x + r0.w;
  r2.w = 9.99999975e-05 + r1.w;
  r2.w = max(0, r2.w);
  r2.w = min(0.000199999995, r2.w);
  r2.w = r2.w * r2.w;
  r2.w = 2000.00012 * r2.w;
  r1.w = max(r2.w, r1.w);
  r2.xyz = r2.xyz * r1.www;
  r0.w = max(9.99999975e-05, r0.w);
  r2.xyz = r2.xyz / r0.www;
  o0.xyz = max(float3(0,0,0), r2.xyz);
  if (cb0[5].x >= 1) {
    r2.xy = float2(0.5,0.5) + -v1.xy;
    r2.xy = r2.xy * cb0[5].yy + v1.xy;
    r2.xy = frac(r2.xy);
    r2.zw = float2(0.5,0.5) + -r2.xy;
    r0.w = dot(r2.zw, r2.zw);
    r0.w = sqrt(r0.w);
    r0.w = -r0.w * 1.41421354 + 1;
    r0.w = log2(r0.w);
    r0.w = cb0[5].z * r0.w;
    r0.w = exp2(r0.w);
    r2.xyzw = t0.Sample(s0_s, r2.xy, int2(0, 0)).xyzw;
    if(injectedData.isClamped != 0.f){
      r2.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r2.xyz) : r2.xyz;
      r2.xyz = rolloffSdr(r2.xyz);
      r2.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r2.xyz) : r2.xyz;
    }
    r1.w = dot(r2.xyz, r2.xyz);
    r1.w = rsqrt(r1.w);
    r4.xyz = r2.xyz * r1.www;
    r4.xyz = cb0[4].www * r4.xyz;
    r2.xyz = max(float3(0,0,0), r2.xyz);
    r2.xyz = min(r2.xyz, r4.xyz);
    r2.xyz = r2.xyz * r3.xyz;
    r1.w = max(r2.y, r2.z);
    r1.w = max(r2.x, r1.w);
    r2.w = -cb0[4].z + r1.w;
    r3.w = 9.99999975e-05 + r2.w;
    r3.w = max(0, r3.w);
    r3.w = min(0.000199999995, r3.w);
    r3.w = r3.w * r3.w;
    r3.w = 2000.00012 * r3.w;
    r2.w = max(r3.w, r2.w);
    r2.xyz = r2.xyz * r2.www;
    r1.w = max(9.99999975e-05, r1.w);
    r2.xyz = r2.xyz / r1.www;
    r2.xyz = max(float3(0,0,0), r2.xyz);
    r2.xyz = r2.xyz * r0.www;
    r2.xyz = cb0[5].www * r2.xyz;
    r0.w = 2;
  } else {
    r2.xyz = float3(0,0,0);
    r0.w = 1;
  }
  r1.w = (int)r0.w;
  //r1.w = cmp(cb0[5].x >= r1.w);
  bool someBool = cb0[5].x >= r1.w;
  if (someBool) {
    r4.xy = float2(0.5,0.5) + -v1.xy;
    r4.xy = cb0[5].yy * r4.xy;
    r4.xy = r4.xy * float2(2,2) + v1.xy;
    r4.xy = frac(r4.xy);
    r4.zw = float2(0.5,0.5) + -r4.xy;
    r2.w = dot(r4.zw, r4.zw);
    r2.w = sqrt(r2.w);
    r2.w = -r2.w * 1.41421354 + 1;
    r2.w = log2(r2.w);
    r2.w = cb0[5].z * r2.w;
    r2.w = exp2(r2.w);
    r4.xyzw = t0.Sample(s0_s, r4.xy, int2(0, 0)).xyzw;
    if(injectedData.isClamped != 0.f){
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r4.xyz) : r4.xyz;
      r4.xyz = rolloffSdr(r4.xyz);
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r4.xyz) : r4.xyz;
    }
    r3.w = dot(r4.xyz, r4.xyz);
    r3.w = rsqrt(r3.w);
    r5.xyz = r4.xyz * r3.www;
    r5.xyz = cb0[4].www * r5.xyz;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = min(r4.xyz, r5.xyz);
    r4.xyz = r4.xyz * r3.xyz;
    r3.w = max(r4.y, r4.z);
    r3.w = max(r4.x, r3.w);
    r4.w = -cb0[4].z + r3.w;
    r5.x = 9.99999975e-05 + r4.w;
    r5.x = max(0, r5.x);
    r5.x = min(0.000199999995, r5.x);
    r5.x = r5.x * r5.x;
    r5.x = 2000.00012 * r5.x;
    r4.w = max(r5.x, r4.w);
    r4.xyz = r4.xyz * r4.www;
    r3.w = max(9.99999975e-05, r3.w);
    r4.xyz = r4.xyz / r3.www;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = r4.xyz * r2.www;
    r2.xyz = r4.xyz * cb0[5].www + r2.xyz;
    r0.w = 3;
  }
  r2.w = (int)r0.w;
  //r2.w = cmp(cb0[5].x >= r2.w);
  someBool = someBool ? cb0[5].x >= r2.w : false;
  if (someBool) {
    r4.xy = float2(0.5,0.5) + -v1.xy;
    r4.xy = cb0[5].yy * r4.xy;
    r4.xy = r4.xy * float2(3,3) + v1.xy;
    r4.xy = frac(r4.xy);
    r4.zw = float2(0.5,0.5) + -r4.xy;
    r2.w = dot(r4.zw, r4.zw);
    r2.w = sqrt(r2.w);
    r2.w = -r2.w * 1.41421354 + 1;
    r2.w = log2(r2.w);
    r2.w = cb0[5].z * r2.w;
    r2.w = exp2(r2.w);
    r4.xyzw = t0.Sample(s0_s, r4.xy, int2(0, 0)).xyzw;
    if(injectedData.isClamped != 0.f){
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r4.xyz) : r4.xyz;
      r4.xyz = rolloffSdr(r4.xyz);
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r4.xyz) : r4.xyz;
    }
    r3.w = dot(r4.xyz, r4.xyz);
    r3.w = rsqrt(r3.w);
    r5.xyz = r4.xyz * r3.www;
    r5.xyz = cb0[4].www * r5.xyz;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = min(r4.xyz, r5.xyz);
    r4.xyz = r4.xyz * r3.xyz;
    r3.w = max(r4.y, r4.z);
    r3.w = max(r4.x, r3.w);
    r4.w = -cb0[4].z + r3.w;
    r5.x = 9.99999975e-05 + r4.w;
    r5.x = max(0, r5.x);
    r5.x = min(0.000199999995, r5.x);
    r5.x = r5.x * r5.x;
    r5.x = 2000.00012 * r5.x;
    r4.w = max(r5.x, r4.w);
    r4.xyz = r4.xyz * r4.www;
    r3.w = max(9.99999975e-05, r3.w);
    r4.xyz = r4.xyz / r3.www;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = r4.xyz * r2.www;
    r2.xyz = r4.xyz * cb0[5].www + r2.xyz;
    r0.w = 4;
  }
  r2.w = (int)r0.w;
  //r2.w = cmp(cb0[5].x >= r2.w);
  someBool = someBool ? cb0[5].x >= r2.w : false;
  if (someBool) {
    r4.xy = float2(0.5,0.5) + -v1.xy;
    r4.xy = cb0[5].yy * r4.xy;
    r4.xy = r4.xy * float2(4,4) + v1.xy;
    r4.xy = frac(r4.xy);
    r4.zw = float2(0.5,0.5) + -r4.xy;
    r2.w = dot(r4.zw, r4.zw);
    r2.w = sqrt(r2.w);
    r2.w = -r2.w * 1.41421354 + 1;
    r2.w = log2(r2.w);
    r2.w = cb0[5].z * r2.w;
    r2.w = exp2(r2.w);
    r4.xyzw = t0.Sample(s0_s, r4.xy, int2(0, 0)).xyzw;
    if(injectedData.isClamped != 0.f){
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r4.xyz) : r4.xyz;
      r4.xyz = rolloffSdr(r4.xyz);
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r4.xyz) : r4.xyz;
    }
    r3.w = dot(r4.xyz, r4.xyz);
    r3.w = rsqrt(r3.w);
    r5.xyz = r4.xyz * r3.www;
    r5.xyz = cb0[4].www * r5.xyz;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = min(r4.xyz, r5.xyz);
    r4.xyz = r4.xyz * r3.xyz;
    r3.w = max(r4.y, r4.z);
    r3.w = max(r4.x, r3.w);
    r4.w = -cb0[4].z + r3.w;
    r5.x = 9.99999975e-05 + r4.w;
    r5.x = max(0, r5.x);
    r5.x = min(0.000199999995, r5.x);
    r5.x = r5.x * r5.x;
    r5.x = 2000.00012 * r5.x;
    r4.w = max(r5.x, r4.w);
    r4.xyz = r4.xyz * r4.www;
    r3.w = max(9.99999975e-05, r3.w);
    r4.xyz = r4.xyz / r3.www;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = r4.xyz * r2.www;
    r2.xyz = r4.xyz * cb0[5].www + r2.xyz;
    r0.w = 5;
  }
  r0.w = (int)r0.w;
  //r0.w = cmp(cb0[5].x >= r0.w);
  someBool = cb0[5].x >= r0.w ? someBool : false;
  if (someBool) {
    r4.xy = float2(0.5,0.5) + -v1.xy;
    r4.xy = cb0[5].yy * r4.xy;
    r4.xy = r4.xy * float2(5,5) + v1.xy;
    r4.xy = frac(r4.xy);
    r4.zw = float2(0.5,0.5) + -r4.xy;
    r0.w = dot(r4.zw, r4.zw);
    r0.w = sqrt(r0.w);
    r0.w = -r0.w * 1.41421354 + 1;
    r0.w = log2(r0.w);
    r0.w = cb0[5].z * r0.w;
    r0.w = exp2(r0.w);
    r4.xyzw = t0.Sample(s0_s, r4.xy, int2(0, 0)).xyzw;
    if(injectedData.isClamped != 0.f){
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::DecodeSafe(r4.xyz) : r4.xyz;
      r4.xyz = rolloffSdr(r4.xyz);
      r4.xyz = injectedData.gammaSpace != 0.f ? renodx::color::srgb::EncodeSafe(r4.xyz) : r4.xyz;
    }
    r1.w = dot(r4.xyz, r4.xyz);
    r1.w = rsqrt(r1.w);
    r5.xyz = r4.xyz * r1.www;
    r5.xyz = cb0[4].www * r5.xyz;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = min(r4.xyz, r5.xyz);
    r4.xyz = r4.xyz * r3.xyz;
    r1.w = max(r4.y, r4.z);
    r1.w = max(r4.x, r1.w);
    r2.w = -cb0[4].z + r1.w;
    r3.w = 9.99999975e-05 + r2.w;
    r3.w = max(0, r3.w);
    r3.w = min(0.000199999995, r3.w);
    r3.w = r3.w * r3.w;
    r3.w = 2000.00012 * r3.w;
    r2.w = max(r3.w, r2.w);
    r4.xyz = r4.xyz * r2.www;
    r1.w = max(9.99999975e-05, r1.w);
    r4.xyz = r4.xyz / r1.www;
    r4.xyz = max(float3(0,0,0), r4.xyz);
    r4.xyz = r4.xyz * r0.www;
    r2.xyz = r4.xyz * cb0[5].www + r2.xyz;
  }
  r4.xy = float2(0.5,0.5) + -v1.xy;
  r0.w = dot(r4.xy, r4.xy);
  r1.w = rsqrt(r0.w);
  r4.xy = r4.xy * r1.ww;
  r4.xy = r4.xy * cb0[6].xx + v1.xy;
  r4.zw = frac(r4.xy);
  r4.zw = float2(0.5,0.5) + -r4.zw;
  r1.w = dot(r4.zw, r4.zw);
  r1.w = sqrt(r1.w);
  r1.w = -r1.w * 1.41421354 + 1;
  r1.w = log2(r1.w);
  r1.w = cb0[6].y * r1.w;
  r1.w = exp2(r1.w);
  r1.w = cb0[6].z * r1.w;
  r4.xyzw = t0.Sample(s0_s, r4.xy, int2(0, 0)).xyzw;
  r4.xyz = r4.xyz * r1.www;
  r1.w = dot(r4.xyz, r4.xyz);
  r1.w = rsqrt(r1.w);
  r5.xyz = r4.xyz * r1.www;
  r5.xyz = cb0[4].www * r5.xyz;
  r4.xyz = max(float3(0,0,0), r4.xyz);
  r4.xyz = min(r4.xyz, r5.xyz);
  r4.xyz = r4.xyz * r3.xyz;
  r1.w = max(r4.y, r4.z);
  r1.w = max(r4.x, r1.w);
  r2.w = -cb0[4].z + r1.w;
  r3.w = 9.99999975e-05 + r2.w;
  r3.w = max(0, r3.w);
  r3.w = min(0.000199999995, r3.w);
  r3.w = r3.w * r3.w;
  r3.w = 2000.00012 * r3.w;
  r2.w = max(r3.w, r2.w);
  r4.xyz = r4.xyz * r2.www;
  r1.w = max(9.99999975e-05, r1.w);
  r4.xyz = r4.xyz / r1.www;
  r4.xyz = max(float3(0,0,0), r4.xyz);
  r2.xyz = r4.xyz + r2.xyz;
  r2.w = 0;
  r0.w = sqrt(r0.w);
  r0.w = 1.41421354 * r0.w;
  r4.xyzw = t1.Sample(s1_s, r0.ww, int2(0, 0)).xyzw;
  o1.xyzw = r4.xyzw * r2.xyzw;
  r1.xyz = cb0[7].yyy * r1.xyz;
  r0.xyz = min(r1.xyz, r0.xyz);
  r0.xyz = r0.xyz * r3.xyz;
  r0.w = max(r0.y, r0.z);
  r0.w = max(r0.x, r0.w);
  r1.x = -cb0[7].x + r0.w;
  r1.y = 9.99999975e-05 + r1.x;
  r1.y = max(0, r1.y);
  r1.y = min(0.000199999995, r1.y);
  r1.y = r1.y * r1.y;
  r1.y = 2000.00012 * r1.y;
  r1.x = max(r1.y, r1.x);
  r0.xyz = r1.xxx * r0.xyz;
  r0.w = max(9.99999975e-05, r0.w);
  r0.xyz = r0.xyz / r0.www;
  r0.xyz = max(float3(0,0,0), r0.xyz);
  r1.xy = float2(-0.5,-0.5) + v1.xy;
  r1.x = dot(r1.xy, r1.xy);
  r1.x = sqrt(r1.x);
  r1.x = -0.75 + r1.x;
  r1.x = -1.33333337 * r1.x;
  r1.x = max(0, r1.x);
  r1.y = r1.x * -2 + 3;
  r1.x = r1.x * r1.x;
  r1.x = r1.y * r1.x;
  r0.w = 1;
  r0.xyzw = r1.xxxx * r0.xyzw;
  o0.w = 1;
  o2.xyzw = r0.xyzw;
  o3.xyzw = r0.xyzw;
  return;
}